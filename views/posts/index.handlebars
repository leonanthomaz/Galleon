<div class="card">
    <div class="card-body">
        <h1> {{title}} </h1>
        <hr>
        <small>Data de publicação: {{date}}</small>
        <hr>
        <p> {{content}} </p>
    </div>
</div>